apiVersion: v1
kind: Secret
metadata:
  name: payment-provider-secrets
  namespace: replay-api
  labels:
    app: replay-api
    component: payments
type: Opaque
stringData:
  # Stripe Configuration
  STRIPE_SECRET_KEY: "sk_test_changeme"
  STRIPE_WEBHOOK_SECRET: "whsec_changeme"
  STRIPE_PUBLISHABLE_KEY: "pk_test_changeme"

  # PayPal Configuration
  PAYPAL_CLIENT_ID: "changeme"
  PAYPAL_CLIENT_SECRET: "changeme"
  PAYPAL_WEBHOOK_ID: "changeme"
  PAYPAL_ENVIRONMENT: "sandbox"  # or "production"

  # Crypto Configuration
  CRYPTO_DEPOSIT_ADDRESS: "0x0000000000000000000000000000000000000000"
  ETH_RPC_URL: "https://eth.llamarpc.com"
  POLYGON_RPC_URL: "https://polygon.llamarpc.com"
  ARBITRUM_RPC_URL: "https://arbitrum.llamarpc.com"
  BASE_RPC_URL: "https://base.llamarpc.com"

  # Treasury wallet for crypto refunds (private key - VERY SENSITIVE)
  # NEVER commit actual private keys - use sealed secrets or external secret management
  TREASURY_WALLET_PRIVATE_KEY: "changeme"
